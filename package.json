{
    "type": "module",
    "private": true,
    "workspaces": ["packages/*"],
    "scripts": {
        "checks": "bun run eslint && bun run tsc && bun run vitest run --passWithNoTests",
        "bundles": "bun run -F '*' bundle",
        "release": "bun run scripts/release"
    },
    "devDependencies": {
        "@monstermann/eslint-config": "^0.4.0",
        "@monstermann/unplugin-tree-shake-import-namespaces": "^0.2.0",
        "@sindresorhus/tsconfig": "^7.0.0",
        "prompts": "^2.4.2",
        "tsdown": "^0.15.0",
        "typescript": "^5.8.3",
        "vitest": "^3.2.4"
    }
}
